language: node_js
node_js:
  - "7.10"

branches:
  only:
    - master

before_script:
  - npm install -g firebase-tools
  - npm i -g

script:
  - audi build && cd functions && npm i

after_success:
  - firebase deploy --token=${FIREBASE_API_TOKEN}
